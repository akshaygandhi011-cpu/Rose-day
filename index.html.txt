<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Happy Rose Day 🌸</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ff5f9e, #ffc1d9);
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', sans-serif;
      color: white;
      overflow: hidden;
    }

    .container {
      text-align: center;
    }

    .rose {
      font-size: 5rem;
      opacity: 0;
      transform: scale(0);
      animation: roseAppear 1.6s ease forwards;
      animation-delay: 0.4s;
    }

    h1 {
      margin-top: 15px;
      font-size: 2.2rem;
      opacity: 0;
      animation: textFade 1.2s ease forwards;
      animation-delay: 1.8s;
    }

    p {
      font-size: 1.1rem;
      margin-top: 8px;
      opacity: 0;
      animation: textFade 1.2s ease forwards;
      animation-delay: 2.4s;
    }

    @keyframes roseAppear {
      0% {
        opacity: 0;
        transform: scale(0) rotate(-25deg);
      }
      100% {
        opacity: 1;
        transform: scale(1) rotate(0deg);
      }
    }

    @keyframes textFade {
      from {
        opacity: 0;
        transform: translateY(12px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>

  <div class="container">
    <div class="rose">🌸</div>
    <h1>Happy Rose Day</h1>
    <p>This pink rose is just for you 💕</p>
  </div>

</body>
</html>
